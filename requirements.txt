Flask==2.3.2          # Flask for creating the web server
flask-cors==3.1.1     # Flask-CORS to handle cross-origin requests
numpy==1.24.2         # Numpy for numerical operations (used in KMeans calculations)
